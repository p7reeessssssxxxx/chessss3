{
  "name": "oxy-chess-relay",
  "version": "1.0.0",
  "description": "oxy chess assistant relay — receives a board FEN from the in-game client, runs Stockfish, serves a private live board view",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
